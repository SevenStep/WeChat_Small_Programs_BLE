<wxs module="utils">
module.exports.max = function(n1, n2) {
  return Math.max(n1, n2)
}
module.exports.len = function(arr) {
  arr = arr || []
  return arr.length
}
</wxs>
<!--
<button class="button1" type="primary" bindtap="openBluetoothAdapter">开始扫描</button> 
<button class="button1" type="primary" bindtap="stopBluetoothDevicesDiscovery">停止扫描</button>
<button class="button1" type="primary" bindtap="closeBluetoothAdapter">结束流程</button>
-->
<button class = "button_roundel" type="">开始扫描</button>

<view class="devices_summary">已发现{{devices.length}}个外围设备</view> 
<scroll-view  class="device_list" scroll-y scroll-with-animation>
 <view wx:for="{{devices}}" wx:key="index"
   data-device-id="{{item.deviceId}}"
   data-name="{{item.name||item.localName}}"
   bindtap="createBLEConnection"
   class="device_item"
   hover-calss="device_item_hover">
    <view style="font-size:20px;color:#333">{{item.name}}</view>
    <view style="font-size:10rpx;">信号强度：{{item.RSSI}}dBm({{utils.max(0,utem.RSSI+100)}}%)</view>
    <view style="font-size:10rpx">UUID:{{item.deviceId}}</view>
    <view style="font-size:10rpx">Service数量：{{utils.len(item.advertisServiceUUIDs)}}</view>
  </view>
</scroll-view>


<view class="connected_info" wx:if="{{connected}}">
  <view>
   <text>已连接到{{name}}  </text>
   <view  class="operation">
 
   <button size="mini" bindtap="closeBLEConnection">断开连接</button>
   </view>  
  
  </view>
 
 
  
  <view style="font-size:30px;margin-top:10px">
    <view>温度值：{{numT}}℃</view>
    <view>湿度值：{{numH}}%</view>
  </view>

</view>